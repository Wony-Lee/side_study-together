import React from "react";
import styled from "@emotion/styled";

const Layout = styled.div`
  text-align: center;
  border: 1px solid yellowgreen;
`;

const FootBar = () => {
  return <Layout>Foot Bar</Layout>;
};

export default FootBar;
